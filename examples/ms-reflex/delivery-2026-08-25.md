# 交付报告 — 毫秒反应局 · 2026-08-25

## 构建

- 创建:官方 Combos 产品 API(`/game/api/v1/projects/v2`),完整规格作为首条 user_query
- 项目 ID:`2092174855371907072`;最终部署 hash:`e90d33fca1e497ecbb9c2ccce5ea0018`
- 迭代次数:**1 次构建,0 修复 turn**

## 独立验证(浏览器自动化)

| 门禁 | 结果 |
|---|---|
| 标题 → 5 轮 → 结算段位卡 全状态机 | ✓(移动+桌面) |
| 抢跑分支 | ✓ 首轮故意抢跑,TOO SOON 红屏出现且该轮被判负(截图 verify-reflex/mobile-falsestart.png) |
| 变绿检测→点击 ×4 轮 | ✓ 全部命中(像素探针:变绿 RGB 67,255,130) |
| 控制台 / pageerror | 0(双视口) |
| 结算卡要素 | 平均毫秒 + 段位(梦游者) + 5 轮逐轮成绩 + "超过 12% 玩家" + **"※演出效果,非真实排行榜"标注** + 再来一局 |
| 390×780 / 1440×900 | 无重叠裁切 |

## 验证方法备注

- 游戏为 **WebGL canvas**(750×1334 backing,无 2d context):`getImageData` 不可用,改用页面截图 + ffmpeg `crop→scale=1:1→rgb24` 单像素探针判定变绿,再注入点击。该方法对任何渲染管线通用。
- 自动化测得平均 1098ms:反映的是"截图轮询延迟 + 点击注入延迟",不是游戏计时错误——游戏毫秒数以变色渲染帧为起点,符合规格。

## 未覆盖(声明)

- ~~假动作轮判定~~ **2026-08-28 收口**:黄灯假闪被像素探针捕获(RGB 255,214,69),假闪期点击正确判抢跑红屏(RGB 181,35,71)✓
- 逐轮成绩在窄屏的换行略显拥挤(1-5 轮数字 wrap),不影响功能
- 计时精度对账(performance.now 起点的帧对齐):未做帧级核验

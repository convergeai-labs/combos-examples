# 交付报告 — 灰烬王座 Ash Throne · 2026-09-16

## 构建

- 创建:官方 Combos 产品创建 API,完整规格(2081 字符单段)作首条 user_query
- **模型:gpt-6-astra + reasoning_effort=max(用户指定"不计成本上最强")**——thread_status 回读确认 `selected_chat_model_key=gpt-6-astra`、`chat_model_selection_mode=manual`、`reasoning_effort=max`
- 项目 ID:`2100064678123753472`;部署 hash:`c387bb548c268bccf1d1dfe3b0ecc17d`
- 构建计划:三步(王座厅+三阶段状态机 → 演出/音效/结算卡 → 验证部署),**一次构建,0 修复 turn**

## 独立验证(浏览器自动化)

| 门禁 | 结果 |
|---|---|
| 标题 → 开战 → 战斗 → 胜利/死亡 → 重燃 全状态机 | ✓(移动+桌面,0 控制台错误) |
| 死亡分支 | ✓ "EVEN ASH REMEMBERS 薪尽于此" + 陨落次数/完美翻滚/承受伤害统计 + 立即重燃 |
| 胜利分支 | ✓ "THE THRONE IS SILENT 王座已空" + 通关时间 + 灰烬终结者称号 + 保存战绩卡(e2e 回调到达) |
| 立即重燃链路 | ✓ phase 回 playing,outcome 重置(首轮正则没匹配"重燃"被抓出,已复核) |
| 真实输入战斗 | ✓ 摇杆拖动+攻击/翻滚按钮,命中出 visual+audio feedback 事件;难度诚实(dash-spam bot 23 秒阵亡) |
| 完美翻滚 | ✓ 统计计数观测到(bot 蒙中 1 次);火花+子弹时间演出未专项验证 |
| 前摇电报 | ✓ 战斗截图可见红色扇形预警区 |
| 匿名可玩(Post 后) | ✓ fresh context 加载正常,0 错误 |

## e2e 钩子(本作的验证为什么顺)

游戏内建 `__combosGameplayTest`(仅 `?e2e=1` 激活):`getSnapshot()` 出 phase/outcome/retry 状态机事实,`invokeAction('victory'/'defeat')` 直达两个结算分支。正常 URL 不暴露。

## 未覆盖(声明)

- **Boss 三阶段 P2/P3 的招式差异与处决慢镜头**:bot 无视觉读招能力,最多活到 23 秒(P1 内),未到达阶段转换;机制存在性以规格+构建计划为据,实测面到人玩为止
- 无伤通关的机制可行性(规格门禁 7):自动化证明不了,需真人高手验证
- 保存战绩卡的导出内容、合成音效的听觉质量
- PC 键盘(WASD/Space/J)路径:本次走 DOM 按钮验证,键盘映射未实测
